# RFC 5322 requires CRLF line endings, and both Cloudflare's local email endpoint and the parsers
# downstream hand the bytes through untouched — so git must never normalise these fixtures.
test/fixtures/*.eml -text
